import { ISend } from "../component";
import s from "./styles.module.scss";
import cn from "classnames";

const ConfirmSend = (props: { transaction: ISend }) => {

  return (
    <div></div>
  );
};

export default ConfirmSend;
